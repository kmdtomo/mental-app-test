# AI音声日記アプリ アプリケーションフロー概要

本ドキュメントでは、本アプリの内部的な処理フローを、以下の5つのステップに分けて解説する。

## 全体フロー図

1. **音声入力・文字起こし**
2. **セグメント化**
3. **感情エンジンによる分析**
4. **AIへのコンテキスト注入**
5. **応答・日記生成**

---

## 1. 音声入力・文字起こし
ユーザーの声をテキストデータとして取り込む最初の工程である。

*   **音声録音**: ブラウザ上でユーザーの音声を録音する（最大60秒）。
*   **音声認識 (Whisper)**: 録音された音声データをOpenAIのWhisper APIに送信し、高精度なテキストデータに変換する。
*   **出力データ**: 句読点を含む生のテキストデータ。

## 2. セグメント化
感情分析の精度を高めるため、テキストと音声を「意味のある最小単位」に分解する。

*   **文単位の分割**: 文字起こしされたテキストを、句読点や文脈に基づいて一文ごとの「セグメント」に分割する。
*   **タイムスタンプの特定**: 各セグメントが音声データの「何秒から何秒まで」に該当するかの開始・終了時間を特定する。
*   **目的**: 60秒の会話の中で移ろいゆく感情の変化（前半は元気だが後半は沈んでいる等）を捉えるための準備工程である。

## 3. 感情エンジンによる分析
本アプリの核となる工程である。言葉の意味ではなく「声のトーン」から感情を数値化する。

*   **音声切り出し**: 特定されたタイムスタンプに基づき、元の音声ファイルから「その一文を話している部分」だけを物理的に切り出す。
*   **感情エンジン (VADモデル)**: 切り出された音声をDeep Learningモデルが解析し、以下の3つの指標（VAD）を算出する。
    *   **Valence (快度)**: ポジティブかネガティブか
    *   **Arousal (覚醒度)**: 興奮しているか落ち着いているか
    *   **Dominance (優位性)**: 能動的か受動的か
*   **ラベルの統一**: 算出された数値(VAD)に基づき、以下の**8つの統一ラベル**のいずれかを付与する。
    *   **ポジティブ系**: `喜び・楽しい`, `穏やか・リラックス`, `落ち着き`
    *   **ネガティブ系**: `ストレス・緊張`, `不安・心配`, `悲しみ`, `疲労・無気力`
    *   **中立**: `中立`

## 4. AIへのコンテキスト注入
AIがユーザーに返答するための情報を構築する。単なるテキストチャットとは異なり、感情データが強制的に組み込まれる。

*   **情報の統合**: 以下の情報をセットにしてAI（GPT-4o-mini）への入力データ（プロンプト）を作成する。
    1.  **発言内容**: ユーザーが何を言ったか（テキスト）
    2.  **感情ラベル**: その時どういう声色だったか（例：「セグメント2は『悲しみ』」）
    3.  **VAD数値**: 感情の強弱やエネルギーレベル
*   **感情認識AIとしての振る舞い**: AIには「ユーザーの発言」だけでなく、「解析された声の感情」が客観的な事実として渡される。これにより、AIはテキスト情報だけでなく、**「今、ユーザーがどのような感情状態で話しているか」を正確に認識した状態**で対話を行うことができる。
    *   例：「大丈夫」という言葉と共に「悲しみ」のラベルが渡された場合、AIはその悲しみの感情を含んだ文脈として理解し、応答を生成する。

## 5. 応答・日記生成
統合された情報を元に、AIがユーザーのメンタルヘルスを支援するアウトプットを行う。

*   **AI応答（チャット）**:
    *   注入された感情コンテキストに基づき、ユーザーの「今の感情」に寄り添った返答を生成する。
    *   例：声に「疲れ」が検出された場合、内容は普通でも「お疲れのようですね」と労う言葉をかける。
*   **日記自動生成**:
    *   一日の終わりに、その日の全ての「会話ログ」と「感情推移データ」を集約する。
    *   出来事の記録だけでなく、客観的な感情スコア（平均VAD値など）を含めた日記と、メンタルヘルス改善のためのインサイト（気づき）を生成して保存する。

---

## まとめ：このアプリの独自性
一般的なボイスメモや日記アプリと異なり、このアプリは**「言語化されない感情（声のトーン）」を数値化・ラベル化し、それをAIの判断材料として強制的に組み込む**ことで、ユーザー自身も気づいていない本音やストレス状態を可視化・ケアするフローを持っている。
